# backend test app (managed)
Go std-lib HTTP server (no deps), built from the Dockerfile into a signed, scanned image.
Deploy as ngris type `managed`; Runtime: Port `8080`, Backend paths `/`. Health path: `/api/health`.
